/**
 * main - Entry point
 *
 *Return: Always 0 (Success)
 */

#include <stdio.h>

int main(void)
{

	int zero;

	for (zero = 0; zero <= 9; zero++)
	{
		printf("%d", zero);
	}
	printf("\n");

	return (0);
}

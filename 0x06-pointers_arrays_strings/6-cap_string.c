#include "holberton.h"
/**
 *cap_string - to capit
 *@s: pointer
 *Return: s
 */
char *cap_string(char *s)
{
	int a;

	for (a = 0; s[a] != '\0'; a++)
	{
	}
	for(;s[a] >= 97 && s[a] <= 122; a++)
	{
	}
	if (s[a - 1] == ',' || s[a - 1] == ';' || s[a - 1] == '.' ||
	    s[a - 1] == '!' || s[a - 1] == '?' || s[a - 1] == '"' ||
	    s[a - 1] == '(' || s[a - 1] == ')';

	    return (s)
	    }
}
